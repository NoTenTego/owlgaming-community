# =============================================================
#  OwlGaming MTA – Konfiguracja środowiska
#  Skopiuj ten plik do .env i uzupełnij wartości
#  cp .env.example .env
# =============================================================

# ------------------------------------------------------------
# SERWER MTA
# ------------------------------------------------------------
# IP serwera (zostaw puste dla 0.0.0.0 / tryb dev)
SERVER_IP=

# Czy serwer ma być widoczny w masterlist MTA? (1 = tak, 0 = nie)
# Dev: 0  |  Prod: 1
SHOULD_BROADCAST=0

# Twój adres e-mail (wymagany do rejestracji serwera)
OWNER_EMAIL_ADDRESS=admin@example.com

# Tryb produkcyjny (0 = dev/test, 1 = produkcja)
PRODUCTION_SERVER=0

# Hasło do UCP (User Control Panel)
WEBSITE_PASSWORD=changeme

# ------------------------------------------------------------
# BAZA DANYCH – MTA (główna)
# ------------------------------------------------------------
MTA_DATABASE_HOST=db
MTA_DATABASE_PORT=3306
MTA_DATABASE_NAME=mta
MTA_DATABASE_USERNAME=mta_user
MTA_DATABASE_PASSWORD=changeme_mta

# ------------------------------------------------------------
# BAZA DANYCH – CORE (konta, grupy)
# ------------------------------------------------------------
CORE_DATABASE_HOST=db
CORE_DATABASE_PORT=3306
CORE_DATABASE_NAME=core
CORE_DATABASE_USERNAME=core_user
CORE_DATABASE_PASSWORD=changeme_core

# Hasło roota MySQL (tylko do docker-compose dev)
MYSQL_ROOT_PASSWORD=changeme_root

# ------------------------------------------------------------
# API KEYS (opcjonalne – zostaw puste jeśli nie używasz)
# ------------------------------------------------------------
# Klucz API forum OwlGaming (forums integration)
FORUMS_API_KEY=

# Klucz klienta Imgur (do podglądu skinów i tekstur)
IMGUR_API_KEY=
